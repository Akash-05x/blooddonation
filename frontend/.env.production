# Production environment — filled in after Vercel deployment
# Replace the URLs below with your actual deployed URLs

# REST API base URL (Vercel backend)
VITE_API_URL=https://blooddonation-1me6.onrender.com/api

# Socket.IO server URL (Railway / Render persistent server)
VITE_SOCKET_URL=https://your-socket-server.railway.app
